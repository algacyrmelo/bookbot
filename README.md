# BookBot
BookBot is my first [Boot.dev](https://www.boot.dev) project!  
It's a simple script that counts words and letters from plain text files.

## Usage
Run the script with the path to the text file:
```bash
python3 main.py <path_to_book>
```
